# 更新日志

格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## 0.2.0 — 2026-08-30

要求内核 0.3.0 及以上：本版用到 `market/:name/setup` 端点，它自内核 0.3.0 起才有。

### 新增

- **面板商店代跑编译。** 按索引的 `setup.scripts` 依次跑装后步骤，且**依赖不缺也跑** ——
  面板插件包的 `dist/` 多半被它自己的仓库 `.gitignore` 掉，而带 node 侧的包
  `webuiPanel.server` 正指向那一层（hardware 就是）。此前只装依赖不编译，装完 node 侧必然
  加载失败，报的是「找不到 dist/index.js」，离真实原因很远。就地拉取拉来新提交之后
  `node_modules` 还在而产物已旧，此时跳过就还是跑着上一版代码，且毫无迹象；远端没有新提交、
  依赖也不缺时整个跳过 —— 那一次更新什么都没改。
- **新增 `src/pm.ts`**，顶掉原先只会装依赖的 `runInstall`。不复用内核那份同名模块：本仓库按
  peer 依赖 npm 上的 core，复用等于「必须先发 core 才能改这里」。
- **市场页新增「装依赖并编译」**（`POST market/:name/setup`），覆盖三种情形：手工放进插件目录
  的插件、装的时候这一步失败过、以及自己 `git pull` 过而产物已旧。
- **主题三态**（跟随系统 / 浅色 / 深色），收在 `web/src/theme.ts`。真正生效的是
  `<html data-theme>` 而非 `prefers-color-scheme`，故三态在此解析成两值再写上去，样式表不必
  知道「跟随系统」这回事。首帧由 `index.html` 的内联脚本同步写一次同样的属性 —— 少了那一手，
  深色偏好的使用者会先看到一帧浅色再跳成深色。
- **宽屏（≥860px）侧栏定高不滚**，顶栏隐去，只有 `.nav` 与 `.main` 各自滚动。

### 变更

- 市场页的安装改走确认框，并把「装依赖并编译」「配置」「主页」「删除」收进「更多」菜单 ——
  安装现在会跑几分钟的编译与下载，不说一声等于让人对着一个像卡住了的界面；而一排五个按钮在
  窄卡上必然折行。
- script 名过白名单正则。Windows 上 `pnpm` 是 `.cmd`，node 拒绝不经 shell 执行它，于是包管理器
  只能带 shell 起 —— 那时参数是被解释的字符串，索引里一个 `build && curl …` 就是任意命令执行。
  名字不合法时丢弃整条 `setup` 而非只跳过那一个：只跳过会装出一个「依赖装了、产物没编译」的包。
- 装依赖失败与装后步骤失败分成 `dependencyError` / `setupError` 两个字段而非合一，因为后手
  不同：前者去目录里执行包管理器，后者去执行那个 script。
- 帮助页的文档列表改为照文档站侧边栏分组：平铺一列时「官方插件」那六页会把另外十条挤没。
- 日志的作用域列超出即省略，完整值放进 `title`；行展开时该列折行而非撑开列宽 —— 撑开会让这
  一行的四列与上下各行错开，一屏日志失去纵向对齐。
- CI 与 `engines.node` 抬到 vite 8 的真实下限（`^20.19.0 || >=22.12.0`）。下限来自 `node:util`
  的 `styleText`（20.12 才有）；原先钉 20.11.0 时 `vite build web` 报的是
  `SyntaxError: does not provide an export named styleText`，与本仓库无关，排查起来很绕。

### 修复

- **`storeResultText` 在装后步骤失败时仍说「刷新页面即可看到组件」。** `!hasServer` 那一支排在
  装后步骤失败判定之前，而组件文件本身就可能是编译产物（由用例抓出）。
- **拖动时页面不跟着滚。** `GridBoard` 的边缘自动滚动写死了 `window`，而宽屏下真正在滚的是
  `.main` —— 表现为把卡片拖到屏幕下沿，页面不动，够不到下面的行。现在起手时沿 DOM 找一次
  「能滚且溢出」的祖先，边缘带也按容器边界算而非视口。

新增 28 条用例，共 561 条。

## 0.1.0 — 2026-08-29

内核不再自带面板，本插件提供概览、账号、日志、插件、插件市场、面板商店、配置与帮助八个页面。
配置表单由各插件声明的 schema 自动生成。

- **node 侧**：面板产物挂载、面板插件扫描与静态挂载、面板插件的配置读写与 node 侧入口加载、
  面板插件商店（git 拉取与依赖安装）。
- **浏览器侧**：12 列栅格的组件板（可拖动、可改尺寸、布局落 localStorage）、schema 驱动的表单
  （分区横向页签、19 种控件）、面板插件的运行期装载与逐包样式限定。

527 条用例，20 个测试文件。
