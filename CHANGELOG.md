# 更新日志

格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## 0.3.3 — 2026-09-14

仍要求内核 0.4.0 及以上，与 0.3.2 相同。

### 修复

- **点「配置」不再打开一张空表单。** 0.3.2 起，已配置账号的编辑框里适配器配置那几项全是空的，
  且标题上还挂着一句「适配器配置已改动，保存后会自动重连该账号」—— **在那个状态下按保存会把
  这个号的地址与凭据清空**。

  起因是 `openEdit()` 用 `structuredClone` 拷配置：`accounts` 是个 `ref`，取到的是 Vue 的
  响应式代理，而克隆代理直接抛 `DataCloneError`。它抛在 `editing.value = item` **之后**，
  于是模态照常打开、配置停在空对象；又因为空对象与已存的配置不同，那句「已改动」跟着亮起 ——
  一处异常生出两个看着无关的症状。改用仓里本就有的 `snapshot()`（JSON 往返），
  `configedit.ts` 的文件头早已记着这个坑，现在调用处也记一份。

### 新增

- **「添加账号」也能填重连策略四项了**，不必建完再进一次配置框。内核的 `POST accounts` 本就
  收 `retry`，且刻意做成一次 `create()` 完成 —— 建完再 PATCH 一次会因 `config` 到达而断开
  重连，一个刚接上的号先闪一次离线。

  这四项**没有移进 `adapter-napcat` 一类的适配器插件**，仍归内核：重连是内核替所有适配器
  统一做的事，适配器不参与。移进去就得要求每个适配器作者各自声明一遍，且哪个适配器忘了写，
  它的账号就没有这个能力，而使用者无从知道为什么。

  两处表单现在共用 `RetryFields.vue`，不是各写一遍 —— 文案里带着「留空即跟随全局，当前为 X」
  这类必须与内核语义对齐的话（尤其 `limit: 0` 是「一直重连」而非「不重连」），抄一份的症状是
  改了一处、另一处继续说着旧话。字段 `id` 改用 `useId()` 生成：模态开着时下方那张表单并未
  卸载，写死 `id` 会得到两个同名元素，点模态里的标题会去聚焦背后那张表单的输入框。

- **扩展页面可以自带图标**（`registerPage({ icon })`），导航与页头都用它；不给或认不出来时
  回落到默认图标。此前每个扩展页面在导航里都是一个一模一样的点，装三个就分不出哪个是哪个。

  收两种写法：整段 `<svg>`（从图标库复制即可）或只给一条 `path` 的 `d`。画布固定 24×24。

  **图标跟着主题变色** —— 这是把形状**内联**进面板自己的 `<svg>`、描边取 `currentColor` 换来的。
  `<img src="icon.svg">` 做不到：那是另一份文档，外面的颜色进不去，只能钉死一个颜色，深色
  主题下多半看不见。内联也顺带免掉一次单独的 HTTP 与一个可能单独失败的静态路径 ——
  几何随 `custom-pages` 那个已带令牌的请求一起到，反代后面行为一致，且不暴露本地路径。

  代价是插件写的 `fill` / `stroke` / `stroke-width` / `opacity` 一律丢掉（颜色归面板），
  `transform` 也丢（它能把形状挪到 24×24 之外，导出时请压平）。

  解析是**白名单**而非过滤黑名单：只收七种形状元素与它们的几何属性，`<script>`、`onload=`、
  `<use href>`、`<foreignObject>`、`url(...)` 不是「被清掉」而是从未被读取 —— 那段 svg 会变成
  面板页面的一部分，而面板那一页上有令牌。有一处不认识即**整枚作废、回落到默认**并记一条警告：
  不逐个跳过是有意的，一个 `<circle>` 少了 `cx` 会画到角上、少了 `r` 会整个不见，两种都是
  「图标显示成了别的东西」，比一枚认得出来的默认图标难查得多。一枚坏图标只损失图标本身，
  不像坏 `src` 那样让整个页面注册作废。

- webui 自己的「消息统计」页用上了这个字段 —— 机制若对自己都不够用，对别人也不会够用。

### 其他

- 新增 `src/pageicon.ts` 与 31 条用例，共 723 条。

## 0.3.2 — 2026-09-13

仍要求内核 0.4.0 及以上 —— `AccountRecord.retry` 自 0.4.0 就在了。有一处用得上内核 0.5.2，
见下文那条注记；装在更旧的内核上时只是那一种边角情形保存不了，其余照常。

### 新增

- **账号编辑框补上重连策略四项**：重连次数上限、首次重连间隔、重连间隔上限、退避倍率。
  内核 0.4.0 起就收 `AccountRecord.retry` 了，但面板一直没给入口 —— 于是「这个号连不上就
  别再试了、那个号一直试下去」这件事只能去改 `config/yunzai.yaml`，而那是一个全局数字，
  满足不了两个诉求。

  **四项各自可以留空，留空即跟随全局**，与内核的逐字段回落对齐。为此有两处刻意的做法：

  - **不预先填上此刻的全局值。** 填了就等于把「跟随全局」偷换成「此刻的全局值」，而后者
    从此不跟着全局改动走 —— 没人记得自己抄过，症状是「我改了全局间隔，这个号却不听」。
    全局值改为写在每一项下方的灰字里（「留空即跟随全局设置，当前为 2 秒」），读得到才写。
  - **表单里四项一律以字符串存，`""` 才是「没填」。** 用数字表达不了这件事：`limit: 0` 是
    「一直重连」、`interval: 0` 是「不等待、立刻重试」，两者都是合法取值，把留空当 0 送上去
    会让一次「我没填这项」变成一条实际生效的策略，而界面上看不出区别。

  四项全空时送 `retry: null`，那正是内核的「清掉覆盖、回到跟随全局」；另配了「四项一并改回
  跟随全局」一个按钮。**填错的值原样回送**，不在面板这侧拦 —— 判据只该有一处（内核会给出
  指名到字段的 400），而悄悄丢掉一个读不懂的值等于替使用者改了配置。

  这四项**不进 `SchemaForm`**，单独成节：那张表由适配器的 `accountSchema` 驱动，而重连是
  内核替所有适配器统一做的事。混进去就得要求每个适配器作者各自声明一遍，于是同一件事有 N 份
  声明，且哪个适配器忘了写，它的账号就没有这个能力。

  **注记：装在内核 0.5.2 之前的版本上时，适配器插件被卸载的账号存不进这四项** —— 那些内核的
  `update()` 无条件要适配器在场，会报「适配器 X 未注册」。适配器正常加载时不受影响。

- **账号列表显示自定义了重连策略的账号**，并列出填了哪几项（「最多 3 次 · 首次等 2 秒」）。
  这是一条看不见的状态，而「这个号为什么不再重试了」的答案往往就在里面 —— 只在编辑框里
  才看得到的话，排查要先逐个点开账号。只列填了的项：跟随全局的项不算这个号自己的事。

### 修复

- **只改备注或重连策略的保存不再踢掉一个在线的号。** 内核收到 `config` 就会断开重连（那是
  对的：地址或 token 变了而 socket 还是旧的，使用者会以为「改了没生效」），而内核 0.4.0 起
  专门留了「只动 `label` / `retry` 就不动连接」的路 —— 面板此前每次保存都捎上 `config`，
  那条路因此永远走不到，一次把上限从 5 改成 10 的保存会让账号断线，那期间的消息全丢。

  现在 `config` 只在真的改过时才送（比 JSON 文本，宁可多判成变了 —— 代价只是多一次重连，
  而漏判的代价是「改了地址却没重连」）。保存按钮的文案随之而变：动过配置才叫「保存并重连」。

### 其他

- 时长的「数值 + 单位」拆解从 `SchemaField.vue` 抽进 `web/src/duration.ts`，与重连那两项
  共用一份判据。**它必须与内核 `parseDuration` 的正则是同一条** —— 分开写的症状是「这个值
  面板收下了、内核却解析不出来」，而内核解析不出来时静默回落到缺省值，于是使用者看到的是
  「我明明填了 5 秒，它却按一分钟退避」。抽出来之后这份判据第一次有了用例。
- `@yunzai-ng/types` 的 peer 版本自 `>=0.2.0` 抬到 `>=0.3.0`：`AccountRetryOverride` 自
  0.3.0 才有，此前那个范围写得比实际宽。

新增 31 条用例，共 692 条。

## 0.3.1 — 2026-09-13

仍要求内核 0.4.0 及以上：本版没有用到新的内核能力。

### 修复

- **插件页的四个管理动作改用安装目录名请求市场。** 市场按安装目录寻址
  （`plugins/<目录名>`，与索引条目同名），而列表里显示的 `name` 是插件
  `definePlugin({ name })` 声明的名字。两者常常不同 —— 中转站签到插件的目录叫
  `relay-checkin-plugin`（仓库名），它自己声明 `relay-checkin`。此前更新、重装、
  装依赖并编译、删除以及「可更新」徽标一律拿声明名去请求，后果是：

  - 报「插件市场中没有名为 relay-checkin 的插件」，而那个插件明明就装在那儿，
    市场页同时还显示着「已安装」（那一侧判的是目录，是对的）
  - 更糟的是内核在查索引**之前**就按这个名字卸载了插件 —— 它从插件列表里凭空消失，
    目录却完好。内核 0.5.1 已不再牵连（失败即装回），但名字本身得在这边改对

  目录名取自内核给的 `root`（安装目录绝对路径），判据抽到 `web/src/market.ts` 的
  `installDirOf()` 并配了用例 —— 两种路径分隔符、末尾多一个分隔符、以及 `root` 为空
  时退回声明名，都是会让请求打到 `market//update` 的那类失手。

  **装在内核 0.5.0 上时有一处降级**：更新那个目录名与声明名不同的插件会成功，但内核
  卸不掉内存里那份旧模块，故更新完还需手动点一次「重载」。0.5.1 起不需要。

- 删除确认框里补一行「要删的目录是 `plugins/<目录名>`」。删除是不可撤销的动作，而此前
  框里只有插件的声明名 —— 恰恰不是要被删掉的那个东西的名字。

新增 4 条用例，共 661 条。

## 0.3.0 — 2026-09-13

要求内核 0.4.0 及以上。本版用到三样自 0.4.0 才有的东西：市场条目的 `installedVersion` /
`updatable`（判「可更新」）、`GET market/:name/update-probe`（更新前探测本地改动）、以及
`POST market/:name/update` 的 `stash` 与 `fresh` 参数。

装在旧内核上不会报错，只是这几处各自降级：徽标不出现、探测 404 之后按「没有改动」发请求、
撞上本地改动时由旧内核照旧自动暂存。

### 变更

- **插件市场与插件页的职责重新划分。** 市场只管安装与浏览，装好之后的事全在插件页。此前
  两个页面都能更新、都能删除，同一件事有两处入口，而它们的按钮排布与文案并不一致。
  - 市场卡片收拢为三个动作：安装、查看、访问仓库。装完只留一句「在插件页查看」。
  - 插件页承担四个管理动作：**更新**、**重装**、**装依赖并编译**、**删除目录**。卡片外只留
    常点的三个（配置、更新、重载），其余进「更多」—— 一排六个按钮在窄卡上必然折行。
  - 「删除目录」与原有的「卸载」并存，文案点明差别：卸载只摘掉内存里那份（重载即回来），
    删除动的是磁盘。
- **更新撞上本地改动时先问一句，不再默默暂存。** 内核 0.4.0 起遇到未提交的改动会中止；面板
  据此在更新前调一次 `update-probe`，`dirty` 为真才弹问句。那个问句**不可关闭**（Esc 与点
  遮罩都不结算），10 秒倒计时后按「暂存并更新」自动结算，故默认结局与从前相同，区别在于
  使用者知道自己的改动去了 `git stash`、以及怎么取回。
- **管理动作不再允许并发。** 新增一个覆盖「确认 → 探测 → 请求」整条流程的锁，任一动作进行
  中其余按钮一律禁用。此前只有「请求飞着的那一段」被禁用，而确认框与探测那两段是空的 ——
  探测那次 GET 没有遮罩兜着，那一刻能点开另一张卡的更新，于是两个问句叠在一起。

### 新增

- **令牌闸门上加「发送到日志」。** 内核 0.4.0 起令牌恒生成（此前仅监听本机时不生成，于是
  「本机免令牌」成了默认），于是每个本机部署首次打开面板都要去日志里抄一串字符 —— 抄丢了
  从前只能翻配置文件。这枚按钮请求 `POST /api/token/reveal` 把令牌重新打进日志一次。
  响应体**不带令牌**：那个端点无需鉴权（需要它的人恰恰没有令牌），带上就等于任何本机页面
  都能取到面板的全部写权限；写进日志则读日志本身另需权限。
- **插件市场加「可更新」页签与按标签筛选**，与面板商店同一形制。这需要内核先补事实：市场条目
  从前只有 `installed` 一个布尔值，判不出可更新。
- **三页的筛选逻辑抽成 `web/src/filter.ts` 共用**：页签判据、角标计数、标签与关键词。两处各写
  一遍的代价不是重复而是**悄悄分叉** —— 一边把作者算进关键词、另一边不算，而两个页面看起来
  一模一样。
- **卡片描述统一限两行**，配一个「查看」模态放全文与卡片上摆不下的事实（来源索引、装后步骤、
  已装版本、命令与中间件明细）。
- **消息统计页**（`webadapter/`）：按小时与按天两种粒度的趋势图，柱形 / 折线 / 面积 / 堆叠柱
  四种画法，收发消息、图片与表情包分开计数，可按账号筛选。

  账号一律以**平台 id**（QQ 号）为准。发侧的 `MessageSentInfo` 给的是 `accountId` ——
  那是内核生成的记录 uuid，与收侧事件给的 `selfId` 不是一回事，直接拿来作键会让同一个号
  裂成两个账号（收到的记在 QQ 号下、发出的记在一串 uuid 下）。取平台 id 还因为它换适配器
  接入时不变，而记录 id 会随「删掉重配一次」而变，拿它作键会让同一个号的历史断成两截。

  图上一格的说明走 SVG 原生 `<title>`，但它只响应悬停，**触屏上一个字都看不到** ——
  而 Termux 部署的人多半就在手机上看这一页。故点选某一格时把同一段文字写进图下的读数行。
  时间恒带日期（`9月12日 13:00`）：横轴标签为了不重叠可以只写 `13`，而「按小时·近 7 天」
  那个视图里 13 点是哪天根本读不出来。

### 修复

- **令牌闸门整块没有样式。** `.gate` 的 `padding: 0 var(--gutter)` 覆盖掉了同元素 `.card` 的
  内边距（`.gate` 在后）。现只设左右内边距，上下留给 `.card`。
- **下拉框与「更多」菜单被卡片裁掉。** 它们绝对定位，而祖先 `.card` 有 `overflow: hidden`。
  改为 `.card:has(.cdd), .card:has(.menu) { overflow: visible }` —— 按内容判定而非逐处挂类，
  `.cdd` 在每个枚举字段上都有，逐处挂是打地鼠。
- **页签上的计数数字在浅色主题下看不见。** `.tag` 自带 `background: var(--panel)`（浅色下是
  白），而激活态那条只改了 `color: inherit` 没改底色，于是白底白字。悬停能看见是因为 hover
  把颜色改回了 `--text`。
- **侧边栏二级项的竖线与圆点并排成了怪符号。** 展开态藏掉圆点（它与竖线表达同一件事），
  折叠态放回来 —— 那时文案隐去，没有图标就是一行空白。
- **图表被横向拉伸。** 根因是 `preserveAspectRatio="none"` 配一个写死 720 宽的 viewBox，实际
  容器宽约 1400px。现按实测容器宽度取 viewBox，并加 `ResizeObserver` 在宽度变化时重画。
- **消息统计页的脚本一行都不执行**（永远停在「读取中…」）。`<script type="module">` 的抓取
  强制走 CORS，而 iframe 没有 `allow-same-origin`（源不透明，`Origin: null`），内核的静态挂载
  不回 `Access-Control-Allow-Origin`，于是模块被静默拦下。改为经典脚本：`msgstats.js` 由
  `scripts/sync-webadapter.mjs` 在构建时从 `dist/` 机械转换（剥 `export`、裹 IIFE、挂全局），
  以保证被测的那份与真跑的那份是同一段代码。
- **面板商店那句关于「更新 webui 会清空面板插件目录」的说明两半都不准确**，已改。实测发现
  两种包命运相反：git clone 装的包自带 `.git`，git 视为嵌套仓库而 `stash` 直接跳过、
  `reset --hard` 也不动它；tarball 装的普通目录才会被收走。而单文件 `.js` 上不了商店的真实
  原因是**安装单位是目录**，与「版本号读不到」无关 —— 它的版本号写在 `export const meta` 里，
  浏览器照读。

新增 96 条用例，共 657 条。

## 0.2.0 — 2026-08-30

要求内核 0.3.0 及以上：本版用到 `market/:name/setup` 端点，它自内核 0.3.0 起才有。

### 新增

- **面板商店代跑编译。** 按索引的 `setup.scripts` 依次跑装后步骤，且**依赖不缺也跑** ——
  面板插件包的 `dist/` 多半被它自己的仓库 `.gitignore` 掉，而带 node 侧的包
  `webuiPanel.server` 正指向那一层（hardware 就是）。此前只装依赖不编译，装完 node 侧必然
  加载失败，报的是「找不到 dist/index.js」，离真实原因很远。就地拉取拉来新提交之后
  `node_modules` 还在而产物已旧，此时跳过就还是跑着上一版代码，且毫无迹象；远端没有新提交、
  依赖也不缺时整个跳过 —— 那一次更新什么都没改。
- **新增 `src/pm.ts`**，顶掉原先只会装依赖的 `runInstall`。不复用内核那份同名模块：本仓库按
  peer 依赖 npm 上的 core，复用等于「必须先发 core 才能改这里」。
- **市场页新增「装依赖并编译」**（`POST market/:name/setup`），覆盖三种情形：手工放进插件目录
  的插件、装的时候这一步失败过、以及自己 `git pull` 过而产物已旧。
- **主题三态**（跟随系统 / 浅色 / 深色），收在 `web/src/theme.ts`。真正生效的是
  `<html data-theme>` 而非 `prefers-color-scheme`，故三态在此解析成两值再写上去，样式表不必
  知道「跟随系统」这回事。首帧由 `index.html` 的内联脚本同步写一次同样的属性 —— 少了那一手，
  深色偏好的使用者会先看到一帧浅色再跳成深色。
- **宽屏（≥860px）侧栏定高不滚**，顶栏隐去，只有 `.nav` 与 `.main` 各自滚动。

### 变更

- 市场页的安装改走确认框，并把「装依赖并编译」「配置」「主页」「删除」收进「更多」菜单 ——
  安装现在会跑几分钟的编译与下载，不说一声等于让人对着一个像卡住了的界面；而一排五个按钮在
  窄卡上必然折行。
- script 名过白名单正则。Windows 上 `pnpm` 是 `.cmd`，node 拒绝不经 shell 执行它，于是包管理器
  只能带 shell 起 —— 那时参数是被解释的字符串，索引里一个 `build && curl …` 就是任意命令执行。
  名字不合法时丢弃整条 `setup` 而非只跳过那一个：只跳过会装出一个「依赖装了、产物没编译」的包。
- 装依赖失败与装后步骤失败分成 `dependencyError` / `setupError` 两个字段而非合一，因为后手
  不同：前者去目录里执行包管理器，后者去执行那个 script。
- 帮助页的文档列表改为照文档站侧边栏分组：平铺一列时「官方插件」那六页会把另外十条挤没。
- 日志的作用域列超出即省略，完整值放进 `title`；行展开时该列折行而非撑开列宽 —— 撑开会让这
  一行的四列与上下各行错开，一屏日志失去纵向对齐。
- CI 与 `engines.node` 抬到 vite 8 的真实下限（`^20.19.0 || >=22.12.0`）。下限来自 `node:util`
  的 `styleText`（20.12 才有）；原先钉 20.11.0 时 `vite build web` 报的是
  `SyntaxError: does not provide an export named styleText`，与本仓库无关，排查起来很绕。

### 修复

- **`storeResultText` 在装后步骤失败时仍说「刷新页面即可看到组件」。** `!hasServer` 那一支排在
  装后步骤失败判定之前，而组件文件本身就可能是编译产物（由用例抓出）。
- **拖动时页面不跟着滚。** `GridBoard` 的边缘自动滚动写死了 `window`，而宽屏下真正在滚的是
  `.main` —— 表现为把卡片拖到屏幕下沿，页面不动，够不到下面的行。现在起手时沿 DOM 找一次
  「能滚且溢出」的祖先，边缘带也按容器边界算而非视口。

新增 28 条用例，共 561 条。

## 0.1.0 — 2026-08-29

内核不再自带面板，本插件提供概览、账号、日志、插件、插件市场、面板商店、配置与帮助八个页面。
配置表单由各插件声明的 schema 自动生成。

- **node 侧**：面板产物挂载、面板插件扫描与静态挂载、面板插件的配置读写与 node 侧入口加载、
  面板插件商店（git 拉取与依赖安装）。
- **浏览器侧**：12 列栅格的组件板（可拖动、可改尺寸、布局落 localStorage）、schema 驱动的表单
  （分区横向页签、19 种控件）、面板插件的运行期装载与逐包样式限定。

527 条用例，20 个测试文件。
